(load "utilis.lsp")
(defun test (l)
 (random-elt l)
 )

(random-elt '(a b c d ))

(defun test-ex0
 " Test the program Eon aseriesofexamples."
X
(init-ex);InitializetheEprogramfirst.
X
5))
(assert(equal(ex34)
				(assert (equal(ex50 )
							0))
				(assert (equal (ex ' x 0 ) 0 ) ) )

